plugin:
    name: sudokuPlugin
    description: A sudoku grid like task implementation, the researcher can setup the grid size

configurationAtrributes:
  -
    name: gridCols
    type: integer
  -
    name: gridRows
    type: integer
    
scoring:
    completedTaskAttributeSubjectAnswerPrefix : answer
    completedTaskAttributeCorrectAnswerPrefix : rightAnswer
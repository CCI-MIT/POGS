name: pluginName
description: Description of the plugin
# Scoring types are:
# - script : for javascript based script scoring calculation. Must implement the taskScore.js file
# - externalService : for external servers doing the calculation. No need to implement or even have the taskScore.js file
#    - parameter: url - the full URL for the scoring server
#    -     url: http://localhost:8082/typingTask
scoring:
    scoringType : script